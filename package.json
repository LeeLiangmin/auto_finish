{
  "name": "auto_finish",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "build:dev": "bun run build.ts --dev",
    "watch": "bun --watch build popup.ts --outdir dist --target browser && bun --watch build content.ts --outdir dist --target browser"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.268"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
